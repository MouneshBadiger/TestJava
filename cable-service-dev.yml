eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/
    enabled: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
